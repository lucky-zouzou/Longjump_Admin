# 修正版验收记录

## 2026-09-16：网页新增用户

- TypeScript 类型检查、自有服务器生产构建、原平台生产构建均通过。
- 完整回归测试：76 项通过，0 失败、0 跳过；包括新账号通过真实 HTTP 登录、岗位及站点渠道权限隔离、跨来源请求拦截和非管理员创建拦截。
- 新增测试覆盖全部9种岗位、初始停用状态、密码长度、必填项、重复邮箱（含大小写及并发重复提交）、备份期间拒绝创建、审计失败时账号及密码一起回滚。
- 确认数据库仅保存密码哈希，创建响应和审计不包含密码，重复创建不会修改原账号。
- 浏览器实测管理员打开“用户管理”、密码确认错误提示、创建成功自动刷新、账号停用／重新启用及新运营账号登录。
- 修正原有 Node 构建路径转换，支持包含中文的项目目录。未新增或修改数据库迁移。

以上均使用隔离临时数据库。首次在受限环境运行 HTTP 测试时，本地端口被限制；允许本地测试服务后完整76项测试通过。本次未连接或部署正式服务器，GitHub 更新后仍需在服务器重新构建并重启应用。

## 2026-09-14：原版本验收

验收日期：2026-09-14。

- `pnpm test`：通过，70 项测试，0 失败、0 跳过。
- TypeScript 类型检查：通过。
- 自有服务器 Node.js 生产构建：通过。
- 原在线平台生产构建：通过。
- 原已应用数据库迁移 0000—0006 保持不变；新增 0007—0009 在隔离数据库执行通过。
- 8 项审查复现问题均加入回归测试并通过。
- 控制并发交错验证审批和驳回互斥、发运数量不超产量。
- 财务验证合并收款多单核销、未分配预收、冲正、关账冻结、客户交接及历史归属。
- 完整备份实测恢复到全新目录：数据库、表行数、外键、完整性和凭证原文件一致；损坏备份被拒绝；已存在目录不被覆盖。
- 真实 Node.js 生产服务 HTTP 验证公司登录、身份伪造拦截、跨来源请求拦截、销售自审拦截、管理员审批、供应链出库、IDR 销售及印尼语 Excel 导出。
- 界面及中／印尼语使用实际 React 组件服务端渲染检查；未进行浏览器逐页视觉验收。

所有测试使用临时数据，不写入正式业务库。本次上线前只读核对：正式采购单、线下订单及退货暂无记录；现有新品项目处于选款阶段。本次代码迁移不会覆盖该项目。

自有服务器、域名、证书与员工初始账号由技术人员按《服务器部署与恢复》配置。本次未连接或改动用户的外部服务器。
